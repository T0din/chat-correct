import React from 'react';
import { Form } from 'semantic-ui-react';

const FormMessage = () => (
    <Form>
        <Form.Input placeholder="Saisissez votre message" />
    </Form>
);

export  default FormMessage;